#tuplas são listas mais simples não possui os mesmos métodos das listas
#tuplas são estáticas não permitem nenhum tipo de alteração

cores = ("amarelo", "roxo", "vermelho")

print(cores)

for cores in cores:
    print(cores)